sudo: false
language: node_js
cache:
  apt: true
  directories:
    - node_modules
node_js: stable
branches:
  only:
    - dev # build dev branch only
before_install:
  - npm install hexo-cli -g
install:
  - npm install
  - npm install hexo-deployer-git --save
before_script:
  - git config user.name "maxxiaobao"
  - git config user.email "maxiuli95@gamil.com"
  - sed -i "s/github_token/${GH_TOKEN}/g" _config.yml || exit 1
script:
  - hexo clean
  - hexo g
  - hexo d

